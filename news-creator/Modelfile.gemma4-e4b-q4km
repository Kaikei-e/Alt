FROM /tmp/gguf/gemma-4-e4b-it-Q4_K_M.gguf

TEMPLATE """{{- $system := "" }}
{{- range .Messages }}
{{- if eq .Role "system" }}
{{- if not $system }}{{ $system = .Content }}
{{- else }}{{ $system = printf "%s\n\n%s" $system .Content }}
{{- end }}
{{- continue }}
{{- else if eq .Role "user" }}<|turn>user
{{- if $system }}
{{ $system }}
{{- $system = "" }}
{{- end }}
{{- else if eq .Role "assistant" }}<|turn>model
{{- end }}
{{ .Content }}<turn|>
{{ end }}<|turn>model
"""

PARAMETER stop <turn|>
