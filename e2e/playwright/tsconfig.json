{
  "//": "Typechecks the shared harness on its own, so a break in _shared is reported once here rather than twelve times across the suites. Each suite's tsconfig.json also includes _shared, which is what catches a helper used in a way its own module never exercises.",
  "extends": "./tsconfig.base.json",
  "include": ["_shared/**/*.ts"]
}
