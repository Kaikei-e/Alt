# Keeps the SHA-pinned actions in .github/workflows current. Dependabot
# rewrites the pinned SHA and its trailing version comment together.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      github-actions:
        patterns:
          - "*"
