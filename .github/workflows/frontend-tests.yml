name: Frontend Unit Tests (Deprecated)

on:
  workflow_dispatch:

jobs:
  info:
    permissions: {}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use .github/workflows/alt-frontend-unit-test.yaml which calls reusable vitest.yaml."
