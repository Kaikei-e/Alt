# TYPE  DATABASE        USER            ADDRESS                 METHOD

# 開発環境用（ローカル接続は許可）
local   all             all                                     trust

# SSL接続を優先、非SSL接続も許可（開発環境のため）
hostssl all             all             0.0.0.0/0               md5
host    all             all             172.0.0.0/8             md5
host    all             all             10.0.0.0/8              md5
host    all             all             192.168.0.0/16          md5