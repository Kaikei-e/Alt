# Development environment values for search-indexer

# Development-specific image settings
image:
  tag: "development"  
  pullPolicy: Never  # INCIDENT 82 FIX: Local image強制使用 (ImagePullBackOff解決)

# Reduced resource requirements for development
resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "250m"
    memory: "256Mi"

# Development environment variables
env:
  DB_HOST: "db.alt-dev.svc.cluster.local"
  LOG_LEVEL: "debug"
  MEILISEARCH_HOST: "http://meilisearch.alt-dev.svc.cluster.local:7700"

# Development namespace
namespace: alt-dev

# Single replica for development
replicaCount: 1