apiVersion: apps/v1
kind: Deployment
metadata:
  name: pre-processor
spec:
  template:
    spec:
      containers:
        - name: pre-processor
          env:
            - name: DB_SSL_MODE
              value: require