apiVersion: apps/v1
kind: Deployment
metadata:
  name: pre-processor
spec:
  template:
    spec:
      containers:
        - name: pre-processor
          env:
            - name: DB_SSL_MODE
              value: require
            - name: DB_SSL_ROOT_CERT
              value: /app/ssl/ca.crt
          volumeMounts:
            - name: ssl-certs
              mountPath: /app/ssl
              readOnly: true
      volumes:
        - name: ssl-certs
          secret:
            secretName: postgres-ssl-certs