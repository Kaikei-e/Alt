apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: alt-database

resources:
  - migrate-deployment.yaml
  - migrate-rbac.yaml
  - ssl-configmap.yaml
  - migrate-env-configmap.yaml
  - migrate-cm0-configmap.yaml
  - migrate-cm1-configmap.yaml
  - secret.yaml

labels:
  - pairs:
      app.kubernetes.io/component: job
      app.kubernetes.io/part-of: migrations