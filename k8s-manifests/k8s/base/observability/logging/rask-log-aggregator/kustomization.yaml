apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: alt-observability

resources:
  - deployment.yaml
  - secret.yaml
  - service.yaml

labels:
  - pairs:
      app.kubernetes.io/component: logging
      app.kubernetes.io/part-of: rask-log-aggregator