apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f compose.yaml --out ./k8s-manifests/
    kompose.version: 1.36.0 (ae2a39403)
    rask.group: rask-log-aggregator
  labels:
    io.kompose.service: rask-log-aggregator
  name: rask-log-aggregator
spec:
  ports:
    - name: "9600"
      port: 9600
      targetPort: 9600
  selector:
    io.kompose.service: rask-log-aggregator
