apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: alt-apps

resources:
  - secret.yaml
  - deployment.yaml
  - configmap.yaml
  - service.yaml

labels:
  - pairs:
      app.kubernetes.io/component: backend
      app.kubernetes.io/part-of: alt-apps