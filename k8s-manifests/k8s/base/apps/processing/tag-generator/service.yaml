apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f compose.yaml --out ./k8s-manifests/
    kompose.version: 1.36.0 (ae2a39403)
    rask.group: tag-generator
  labels:
    io.kompose.service: tag-generator
  name: tag-generator
spec:
  ports:
    - name: "9400"
      port: 9400
      targetPort: 9400
  selector:
    io.kompose.service: tag-generator
