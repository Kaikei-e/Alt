"""Evaluation harness for Acolyte report generation.

Gate language / citation behaviour changes on metric deltas instead of
ad-hoc inspection. Usage:

    uv run python -m evaluation.run_eval --dataset datasets/baseline.jsonl
"""
