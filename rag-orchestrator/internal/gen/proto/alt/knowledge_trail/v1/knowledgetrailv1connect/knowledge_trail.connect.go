// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: alt/knowledge_trail/v1/knowledge_trail.proto

package knowledgetrailv1connect

import (
	v1 "alt/gen/proto/alt/knowledge_trail/v1"
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// KnowledgeTrailServiceName is the fully-qualified name of the KnowledgeTrailService service.
	KnowledgeTrailServiceName = "alt.knowledge_trail.v1.KnowledgeTrailService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// KnowledgeTrailServiceGetTrailProcedure is the fully-qualified name of the KnowledgeTrailService's
	// GetTrail RPC.
	KnowledgeTrailServiceGetTrailProcedure = "/alt.knowledge_trail.v1.KnowledgeTrailService/GetTrail"
	// KnowledgeTrailServiceResolveBranchProcedure is the fully-qualified name of the
	// KnowledgeTrailService's ResolveBranch RPC.
	KnowledgeTrailServiceResolveBranchProcedure = "/alt.knowledge_trail.v1.KnowledgeTrailService/ResolveBranch"
	// KnowledgeTrailServiceEmitTrailOutcomeProcedure is the fully-qualified name of the
	// KnowledgeTrailService's EmitTrailOutcome RPC.
	KnowledgeTrailServiceEmitTrailOutcomeProcedure = "/alt.knowledge_trail.v1.KnowledgeTrailService/EmitTrailOutcome"
	// KnowledgeTrailServiceSearchTrailProcedure is the fully-qualified name of the
	// KnowledgeTrailService's SearchTrail RPC.
	KnowledgeTrailServiceSearchTrailProcedure = "/alt.knowledge_trail.v1.KnowledgeTrailService/SearchTrail"
)

// KnowledgeTrailServiceClient is a client for the alt.knowledge_trail.v1.KnowledgeTrailService
// service.
type KnowledgeTrailServiceClient interface {
	// GetTrail returns the user's footprint spine in reverse-chronological order.
	GetTrail(context.Context, *connect.Request[v1.GetTrailRequest]) (*connect.Response[v1.GetTrailResponse], error)
	// ResolveBranch records the user's response to a proposed branch (taken or
	// dismissed). Idempotent via a client-supplied UUIDv7. Taking a branch closes
	// the loop: the resolution drops the branch from the open set and the user's
	// engagement with the target re-surfaces as a footprint (return-diff).
	ResolveBranch(context.Context, *connect.Request[v1.ResolveBranchRequest]) (*connect.Response[v1.ResolveBranchResponse], error)
	// EmitTrailOutcome records the observed consequence of a taken branch: the
	// raw visible dwell on the article the branch opened. Raw measurement only —
	// engagement classification is a projector-side derivation, never baked into
	// the emitted fact. One outcome per branch (first write wins server-side),
	// so retries need no client-minted id.
	EmitTrailOutcome(context.Context, *connect.Request[v1.EmitTrailOutcomeRequest]) (*connect.Response[v1.EmitTrailOutcomeResponse], error)
	// SearchTrail is the trail's single rediscovery instrument (D25): full-text
	// search over what the user has actually read (title/content/tags via the
	// article index, intersected with the user's spine). Hits come back as their
	// containing episodes so every result keeps its time context.
	SearchTrail(context.Context, *connect.Request[v1.SearchTrailRequest]) (*connect.Response[v1.SearchTrailResponse], error)
}

// NewKnowledgeTrailServiceClient constructs a client for the
// alt.knowledge_trail.v1.KnowledgeTrailService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewKnowledgeTrailServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) KnowledgeTrailServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	knowledgeTrailServiceMethods := v1.File_alt_knowledge_trail_v1_knowledge_trail_proto.Services().ByName("KnowledgeTrailService").Methods()
	return &knowledgeTrailServiceClient{
		getTrail: connect.NewClient[v1.GetTrailRequest, v1.GetTrailResponse](
			httpClient,
			baseURL+KnowledgeTrailServiceGetTrailProcedure,
			connect.WithSchema(knowledgeTrailServiceMethods.ByName("GetTrail")),
			connect.WithClientOptions(opts...),
		),
		resolveBranch: connect.NewClient[v1.ResolveBranchRequest, v1.ResolveBranchResponse](
			httpClient,
			baseURL+KnowledgeTrailServiceResolveBranchProcedure,
			connect.WithSchema(knowledgeTrailServiceMethods.ByName("ResolveBranch")),
			connect.WithClientOptions(opts...),
		),
		emitTrailOutcome: connect.NewClient[v1.EmitTrailOutcomeRequest, v1.EmitTrailOutcomeResponse](
			httpClient,
			baseURL+KnowledgeTrailServiceEmitTrailOutcomeProcedure,
			connect.WithSchema(knowledgeTrailServiceMethods.ByName("EmitTrailOutcome")),
			connect.WithClientOptions(opts...),
		),
		searchTrail: connect.NewClient[v1.SearchTrailRequest, v1.SearchTrailResponse](
			httpClient,
			baseURL+KnowledgeTrailServiceSearchTrailProcedure,
			connect.WithSchema(knowledgeTrailServiceMethods.ByName("SearchTrail")),
			connect.WithClientOptions(opts...),
		),
	}
}

// knowledgeTrailServiceClient implements KnowledgeTrailServiceClient.
type knowledgeTrailServiceClient struct {
	getTrail         *connect.Client[v1.GetTrailRequest, v1.GetTrailResponse]
	resolveBranch    *connect.Client[v1.ResolveBranchRequest, v1.ResolveBranchResponse]
	emitTrailOutcome *connect.Client[v1.EmitTrailOutcomeRequest, v1.EmitTrailOutcomeResponse]
	searchTrail      *connect.Client[v1.SearchTrailRequest, v1.SearchTrailResponse]
}

// GetTrail calls alt.knowledge_trail.v1.KnowledgeTrailService.GetTrail.
func (c *knowledgeTrailServiceClient) GetTrail(ctx context.Context, req *connect.Request[v1.GetTrailRequest]) (*connect.Response[v1.GetTrailResponse], error) {
	return c.getTrail.CallUnary(ctx, req)
}

// ResolveBranch calls alt.knowledge_trail.v1.KnowledgeTrailService.ResolveBranch.
func (c *knowledgeTrailServiceClient) ResolveBranch(ctx context.Context, req *connect.Request[v1.ResolveBranchRequest]) (*connect.Response[v1.ResolveBranchResponse], error) {
	return c.resolveBranch.CallUnary(ctx, req)
}

// EmitTrailOutcome calls alt.knowledge_trail.v1.KnowledgeTrailService.EmitTrailOutcome.
func (c *knowledgeTrailServiceClient) EmitTrailOutcome(ctx context.Context, req *connect.Request[v1.EmitTrailOutcomeRequest]) (*connect.Response[v1.EmitTrailOutcomeResponse], error) {
	return c.emitTrailOutcome.CallUnary(ctx, req)
}

// SearchTrail calls alt.knowledge_trail.v1.KnowledgeTrailService.SearchTrail.
func (c *knowledgeTrailServiceClient) SearchTrail(ctx context.Context, req *connect.Request[v1.SearchTrailRequest]) (*connect.Response[v1.SearchTrailResponse], error) {
	return c.searchTrail.CallUnary(ctx, req)
}

// KnowledgeTrailServiceHandler is an implementation of the
// alt.knowledge_trail.v1.KnowledgeTrailService service.
type KnowledgeTrailServiceHandler interface {
	// GetTrail returns the user's footprint spine in reverse-chronological order.
	GetTrail(context.Context, *connect.Request[v1.GetTrailRequest]) (*connect.Response[v1.GetTrailResponse], error)
	// ResolveBranch records the user's response to a proposed branch (taken or
	// dismissed). Idempotent via a client-supplied UUIDv7. Taking a branch closes
	// the loop: the resolution drops the branch from the open set and the user's
	// engagement with the target re-surfaces as a footprint (return-diff).
	ResolveBranch(context.Context, *connect.Request[v1.ResolveBranchRequest]) (*connect.Response[v1.ResolveBranchResponse], error)
	// EmitTrailOutcome records the observed consequence of a taken branch: the
	// raw visible dwell on the article the branch opened. Raw measurement only —
	// engagement classification is a projector-side derivation, never baked into
	// the emitted fact. One outcome per branch (first write wins server-side),
	// so retries need no client-minted id.
	EmitTrailOutcome(context.Context, *connect.Request[v1.EmitTrailOutcomeRequest]) (*connect.Response[v1.EmitTrailOutcomeResponse], error)
	// SearchTrail is the trail's single rediscovery instrument (D25): full-text
	// search over what the user has actually read (title/content/tags via the
	// article index, intersected with the user's spine). Hits come back as their
	// containing episodes so every result keeps its time context.
	SearchTrail(context.Context, *connect.Request[v1.SearchTrailRequest]) (*connect.Response[v1.SearchTrailResponse], error)
}

// NewKnowledgeTrailServiceHandler builds an HTTP handler from the service implementation. It
// returns the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewKnowledgeTrailServiceHandler(svc KnowledgeTrailServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	knowledgeTrailServiceMethods := v1.File_alt_knowledge_trail_v1_knowledge_trail_proto.Services().ByName("KnowledgeTrailService").Methods()
	knowledgeTrailServiceGetTrailHandler := connect.NewUnaryHandler(
		KnowledgeTrailServiceGetTrailProcedure,
		svc.GetTrail,
		connect.WithSchema(knowledgeTrailServiceMethods.ByName("GetTrail")),
		connect.WithHandlerOptions(opts...),
	)
	knowledgeTrailServiceResolveBranchHandler := connect.NewUnaryHandler(
		KnowledgeTrailServiceResolveBranchProcedure,
		svc.ResolveBranch,
		connect.WithSchema(knowledgeTrailServiceMethods.ByName("ResolveBranch")),
		connect.WithHandlerOptions(opts...),
	)
	knowledgeTrailServiceEmitTrailOutcomeHandler := connect.NewUnaryHandler(
		KnowledgeTrailServiceEmitTrailOutcomeProcedure,
		svc.EmitTrailOutcome,
		connect.WithSchema(knowledgeTrailServiceMethods.ByName("EmitTrailOutcome")),
		connect.WithHandlerOptions(opts...),
	)
	knowledgeTrailServiceSearchTrailHandler := connect.NewUnaryHandler(
		KnowledgeTrailServiceSearchTrailProcedure,
		svc.SearchTrail,
		connect.WithSchema(knowledgeTrailServiceMethods.ByName("SearchTrail")),
		connect.WithHandlerOptions(opts...),
	)
	return "/alt.knowledge_trail.v1.KnowledgeTrailService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case KnowledgeTrailServiceGetTrailProcedure:
			knowledgeTrailServiceGetTrailHandler.ServeHTTP(w, r)
		case KnowledgeTrailServiceResolveBranchProcedure:
			knowledgeTrailServiceResolveBranchHandler.ServeHTTP(w, r)
		case KnowledgeTrailServiceEmitTrailOutcomeProcedure:
			knowledgeTrailServiceEmitTrailOutcomeHandler.ServeHTTP(w, r)
		case KnowledgeTrailServiceSearchTrailProcedure:
			knowledgeTrailServiceSearchTrailHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedKnowledgeTrailServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedKnowledgeTrailServiceHandler struct{}

func (UnimplementedKnowledgeTrailServiceHandler) GetTrail(context.Context, *connect.Request[v1.GetTrailRequest]) (*connect.Response[v1.GetTrailResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("alt.knowledge_trail.v1.KnowledgeTrailService.GetTrail is not implemented"))
}

func (UnimplementedKnowledgeTrailServiceHandler) ResolveBranch(context.Context, *connect.Request[v1.ResolveBranchRequest]) (*connect.Response[v1.ResolveBranchResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("alt.knowledge_trail.v1.KnowledgeTrailService.ResolveBranch is not implemented"))
}

func (UnimplementedKnowledgeTrailServiceHandler) EmitTrailOutcome(context.Context, *connect.Request[v1.EmitTrailOutcomeRequest]) (*connect.Response[v1.EmitTrailOutcomeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("alt.knowledge_trail.v1.KnowledgeTrailService.EmitTrailOutcome is not implemented"))
}

func (UnimplementedKnowledgeTrailServiceHandler) SearchTrail(context.Context, *connect.Request[v1.SearchTrailRequest]) (*connect.Response[v1.SearchTrailResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("alt.knowledge_trail.v1.KnowledgeTrailService.SearchTrail is not implemented"))
}
