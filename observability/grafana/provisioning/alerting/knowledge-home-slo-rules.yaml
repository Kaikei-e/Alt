# Canonical Knowledge Home SLO alerting is Prometheus
# (observability/prometheus/rules/knowledge-home-slo-alerts.yml).
# Grafana Unified Alerting copies were removed so only one evaluator pages.
# Do not re-add rules here: a paused copy with noDataState: OK silently
# treats missing series as healthy if someone unpauses it.
apiVersion: 1
groups: []
