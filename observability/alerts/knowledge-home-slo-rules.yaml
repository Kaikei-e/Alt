# Not provisioned. Canonical Knowledge Home SLO alerting is Prometheus
# (observability/prometheus/rules/knowledge-home-slo-alerts.yml).
# This file used to hold Grafana Unified Alerting copies (noDataState: OK).
# Those duplicates are removed so a manual import cannot double-page.
apiVersion: 1
groups: []
